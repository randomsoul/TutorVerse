'use client';
import { useState } from 'react'; import { Header, Footer } from '../components/site'; import { classes, competitiveExams, subjects, teachingModes } from '../data';
export default function Become(){const [sent,setSent]=useState(false);const [grades,setGrades]=useState<string[]>([]);const [exams,setExams]=useState<string[]>([]);const [subs,setSubs]=useState<string[]>([]);function toggle(v:string,setter:React.Dispatch<React.SetStateAction<string[]>>){setter(a=>a.includes(v)?a.filter(x=>x!==v):[...a,v])}function submit(e:React.FormEvent){e.preventDefault();setSent(true)}return <><Header/><main className="form-page"><div className="form-intro"><div className="eyebrow">For educators</div><h1>Join the Saral Vigyan tutor network.</h1><p>Registration is an application, not automatic publication. Saral Vigyan reviews every application and decides which tutors are presented to students.</p></div>{sent?<div className="form-card success-card"><div className="success">✓</div><h2>Application received.</h2><p>Your teaching profile has been captured for admin review. Approved tutors can later be matched to managed student requests.</p><a href="/" className="btn primary">Back home →</a></div>:<form className="form-card" onSubmit={submit}><div className="two"><label>Full name<input required placeholder="Your name"/></label><label>Email<input required type="email" placeholder="you@example.com"/></label></div><div className="two"><label>Phone / WhatsApp<input required placeholder="10-digit mobile number"/></label><label>Experience<select required defaultValue=""><option value="" disabled>Select experience</option><option>0–2 years</option><option>3–5 years</option><option>6–10 years</option><option>10+ years</option><option>10+ years</option></select></label></div><fieldset><legend className="text-xs font-extrabold uppercase tracking-wider text-slate-500">Grades / classes you teach</legend><div className="mt-3 grid grid-cols-2 gap-2 sm:grid-cols-4">{classes.map(v=><label key={v} className="flex items-center gap-2 rounded-xl bg-slate-50 p-3 text-xs font-semibold"><input type="checkbox" checked={grades.includes(v)} onChange={()=>toggle(v,setGrades)}/>{v}</label>)}</div></fieldset><fieldset className="mt-6"><legend className="text-xs font-extrabold uppercase tracking-wider text-slate-500">Subjects</legend><div className="mt-3 grid grid-cols-2 gap-2 sm:grid-cols-3">{subjects.map(v=><label key={v} className="flex items-center gap-2 rounded-xl bg-slate-50 p-3 text-xs font-semibold"><input type="checkbox" checked={subs.includes(v)} onChange={()=>toggle(v,setSubs)}/>{v}</label>)}</div></fieldset><fieldset className="mt-6"><legend className="text-xs font-extrabold uppercase tracking-wider text-slate-500">Competitive exams</legend><p className="mt-1 text-xs text-slate-400">Select any exams you prepare students for.</p><div className="mt-3 grid grid-cols-2 gap-2 sm:grid-cols-3">{competitiveExams.map(v=><label key={v} className="flex items-center gap-2 rounded-xl bg-slate-50 p-3 text-xs font-semibold"><input type="checkbox" checked={exams.includes(v)} onChange={()=>toggle(v,setExams)}/>{v}</label>)}</div></fieldset><label>Teaching mode<select required defaultValue=""><option value="" disabled>Select</option>{teachingModes.map(v=><option key={v}>{v}</option>)}</select></label><div className="two"><label>City<input required placeholder="Mumbai / Navi Mumbai / Thane"/></label><label>Preferred area<input placeholder="e.g. Sion, Vashi, Thane West"/></label></div><label>About you<textarea required rows={5} placeholder="Qualifications, teaching approach, achievements and anything Saral Vigyan should know."/></label><button className="btn primary">Submit tutor application →</button></form>}</main><Footer/></>}
