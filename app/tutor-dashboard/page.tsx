'use client';
import { useState } from 'react'; import Link from 'next/link'; import { Header, Footer } from '../components/site';
export default function TutorDashboard(){const [started,setStarted]=useState(false);const [closed,setClosed]=useState(false);const [report,setReport]=useState(false);return <><Header/><main className="mx-auto max-w-7xl px-5 py-10"><p className="text-sm font-bold uppercase tracking-[.2em] text-slate-400">Tutor dashboard</p><div className="mt-2 flex flex-col justify-between gap-3 md:flex-row md:items-end"><div><h1 className="text-4xl font-black tracking-tight">Today&apos;s classroom control.</h1><p className="mt-2 text-slate-500">The tutor confirms class start and closure. TutorVerse then creates the parent notification event.</p></div><Link href="/admin" className="rounded-xl border border-slate-200 px-4 py-2 text-sm font-bold">Admin control →</Link></div><section className="mt-8 grid gap-5 lg:grid-cols-[1.4fr_1fr]"><div className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Next class</p><h2 className="mt-2 text-2xl font-black">Physics · Newton&apos;s Laws</h2><p className="mt-1 text-sm text-slate-500">Aarav Sharma · Class XI · Home tuition · 5:30–6:30 PM</p></div><span className={`rounded-full px-3 py-2 text-xs font-bold ${closed?'bg-slate-100 text-slate-500':started?'bg-emerald-100 text-emerald-700':'bg-amber-100 text-amber-700'}`}>{closed?'Class closed':started?'Class in progress':'Scheduled'}</span></div><div className="mt-7 grid gap-3 sm:grid-cols-2"><button disabled={started||closed} onClick={()=>setStarted(true)} className="rounded-2xl bg-slate-950 px-5 py-4 text-left font-black text-white disabled:opacity-40">✓ Class has begun<p className="mt-1 text-xs font-normal text-slate-300">Confirms student reached the tutor and class started.</p></button><button disabled={!started||closed} onClick={()=>setClosed(true)} className="rounded-2xl border border-slate-200 px-5 py-4 text-left font-black disabled:opacity-40">✓ Class has closed<p className="mt-1 text-xs font-normal text-slate-500">Records closure and triggers parent intimation.</p></button></div>{started&&<p className="mt-4 rounded-xl bg-emerald-50 p-3 text-sm font-semibold text-emerald-800">✓ Start event recorded. Parent notification would be sent through WhatsApp + email in production.</p>}{closed&&<p className="mt-2 rounded-xl bg-blue-50 p-3 text-sm font-semibold text-blue-800">✓ Closure event recorded. Attendance confirmation and class-closure intimation are ready for the parent.</p>}</div><div className="rounded-3xl bg-slate-950 p-6 text-white"><p className="text-xs font-bold uppercase tracking-[.2em] text-slate-500">After class</p><h2 className="mt-2 text-2xl font-black">Class report</h2><p className="mt-2 text-sm leading-6 text-slate-300">Record topic, what was covered, homework and remarks. Parents and students see the report; parent feedback routes to admin.</p><button onClick={()=>setReport(true)} className="mt-5 rounded-xl bg-white px-4 py-3 text-sm font-black text-slate-950">Open class report →</button>{report&&<div className="mt-4 rounded-2xl bg-white/10 p-4 text-sm">Report form selected. Production version will save the report to the classroom and notify the parent when published.</div>}</div></section><section className="mt-6 rounded-3xl border border-slate-200 bg-white p-6"><h2 className="text-xl font-black">Upcoming classes</h2><div className="mt-4 grid gap-3 md:grid-cols-3">{['Chemistry · Tomorrow 6 PM · Online','Mathematics · Friday 7 PM · Batch','Physics · Saturday 5:30 PM · Home'].map(x=><div key={x} className="rounded-2xl bg-slate-50 p-4 text-sm font-semibold">{x}<p className="mt-1 text-xs font-normal text-slate-400">Parent reminder automatically scheduled 30 minutes before.</p></div>)}</div></section></main><Footer/></>}
