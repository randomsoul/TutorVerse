import Link from 'next/link'; import { Header, Footer } from '../components/site'; import { subjects } from '../data';
export default function Subjects(){return <><Header/><main className="page"><div className="page-intro"><div><div className="eyebrow">Subjects</div><h1>Choose what you want to master.</h1><p>Browse tutors by subject and start with the goal that matters to you.</p></div></div><div className="subject-grid big">{subjects.map((s,i)=><Link className="subject-card" href={'/tutors?subject='+encodeURIComponent(s)} key={s}><span className="number">0{i+1}</span><h3>{s}</h3><p>Explore tutors →</p><span className="arrow">↗</span></Link>)}</div></main><Footer/></>}
