import Link from 'next/link'; import { Header, Footer } from '../components/site'; import { subjects, competitiveExams } from '../data';
export default function Subjects(){return <><Header/><main className="page"><div className="page-intro"><div><div className="eyebrow">Learning catalogue</div><h1>Subjects and competitive exams are separate tracks.</h1><p>Academic subjects describe what a student studies. Competitive exams describe the target examination. TutorVerse can link both to the same classroom, tutor and assessment journey.</p></div></div><section><div className="eyebrow">School & academic subjects</div><h2 className="mt-3 mb-6 text-3xl font-black tracking-tight">Subjects</h2><div className="subject-grid big">{subjects.map((s,i)=><Link className="subject-card" href={'/tutors?subject='+encodeURIComponent(s)} key={s}><span className="number">{String(i+1).padStart(2,'0')}</span><h3>{s}</h3><p>Explore learning support →</p><span className="arrow">↗</span></Link>)}</div></section><section className="mt-16"><div className="eyebrow">Competitive exams</div><h2 className="mt-3 mb-6 text-3xl font-black tracking-tight">Exam preparation</h2><div className="subject-grid big">{competitiveExams.map((s,i)=><Link className="subject-card" href={'/tutors?exam='+encodeURIComponent(s)} key={s}><span className="number">{String(i+1).padStart(2,'0')}</span><h3>{s}</h3><p>Explore preparation support →</p><span className="arrow">↗</span></Link>)}</div></section></main><Footer/></>}
