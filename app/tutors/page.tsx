'use client';
import { useMemo, useState } from 'react'; import Link from 'next/link'; import { Header, Footer } from '../components/site'; import { tutors, subjects, classes, boards } from '../data';
export default function Tutors(){const [q,setQ]=useState('');const [subject,setSubject]=useState('');const [grade,setGrade]=useState('');const [board,setBoard]=useState('');const [mode,setMode]=useState('');const filtered=useMemo(()=>tutors.filter(t=>[t.name,t.subject,...t.subjects,t.city].join(' ').toLowerCase().includes(q.toLowerCase())&&(!subject||t.subjects.includes(subject)||t.classes.includes(subject))&&(!grade||t.classes.includes(grade))&&(!board||t.board===board)&&(!mode||t.mode.includes(mode))),[q,subject,grade,board,mode]);return <><Header/><main className="page"><div className="page-intro"><div><div className="eyebrow">Tutor directory</div><h1>Find your person.</h1><p>Search, compare and choose a tutor that matches your goals.</p></div><span className="count">{filtered.length} matches</span></div><div className="filters"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search name, subject or city…"/><select value={subject} onChange={e=>setSubject(e.target.value)}><option value="">All subjects</option>{subjects.map(x=><option key={x}>{x}</option>)}</select><select value={grade} onChange={e=>setGrade(e.target.value)}><option value="">All classes</option>{classes.map(x=><option key={x}>{x}</option>)}</select><select value={board} onChange={e=>setBoard(e.target.value)}><option value="">All boards</option>{boards.map(x=><option key={x}>{x}</option>)}</select><select value={mode} onChange={e=>setMode(e.target.value)}><option value="">Any mode</option><option>Online</option><option>Offline</option></select></div><div className="tutor-grid">{filtered.map(t=><article className="tutor-card" key={t.id}><div className={'avatar '+t.accent}>{t.initials}</div><div className="tutor-meta"><div className="rating">★ {t.rating} <span>({t.reviews})</span></div><h2>{t.name}</h2><b>{t.subject}</b><p>{t.bio}</p><div className="chips">{t.classes.slice(0,3).map(x=><span key={x}>{x}</span>)}</div><div className="card-bottom"><span>₹{t.price}<small>/ class</small></span><Link href={'/tutors/'+t.id}>View profile →</Link></div></div></article>)}</div>{!filtered.length&&<div className="empty"><h2>No tutors match those filters.</h2><p>Try removing a filter or searching for another subject.</p><button className="btn primary" onClick={()=>{setQ('');setSubject('');setGrade('');setBoard('');setMode('')}}>Clear filters</button></div>}</main><Footer/></>}
